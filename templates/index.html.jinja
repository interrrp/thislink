<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Thislink</title>

  <style>
    :root {
      --color-bg-0: #0B0911;
      --color-fg-0: #E4E0EB;

      --color-bg-1: #171221;
      --color-border-1: #251D35;

      --color-accent: #9059FF;
      --color-accent-light: #9059FF20;
      --color-accent-fg: #0B0911;

      --color-error: #ff5454;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      border: none;
      font: inherit;
      color: inherit;
    }

    body {
      font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
      font-size: 16px;

      background-color: var(--color-bg-0);
      color: var(--color-fg-0);

      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    input,
    button {
      outline: none;
      padding: 1rem 1.25rem;
      border-radius: 0.5rem;
    }

    input {
      width: 100%;
      background-color: var(--color-bg-1);
      border: 1px solid var(--color-border-1);
    }

    input:focus {
      box-shadow: 0 0 0 0.25rem var(--color-accent-light);
    }

    button {
      width: fit-content;
      background-color: var(--color-accent);
      color: var(--color-accent-fg);
      cursor: pointer;
    }

    main {
      width: 100%;
      max-width: 50rem;
      padding: 2rem;
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    form {
      display: flex;
      gap: 0.5rem;
    }

    a {
      color: var(--color-accent);
      text-decoration: none;
    }

    input,
    button,
    a {
      transition-duration: 200ms;
    }

    button:hover,
    a:hover {
      filter: brightness(75%);
    }

    .error {
      color: var(--color-error);
    }
  </style>
</head>

<body>
  <main>
    {% if error %}
    <p class="error">{{ error }}</p>
    {% endif %}

    {% if shortened %}
    <a href="{{ shortened }}">{{ shortened }}</a>
    {% endif %}

    <form id="form" action="/" method="post">
      <input id="form-url" name="url" placeholder="Paste or type URL to shorten" />
      <button id="shorten-btn">Shorten</button>
    </form>
  </main>
</body>

</html>
